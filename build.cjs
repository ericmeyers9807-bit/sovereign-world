const fs=require('node:fs');const path=require('node:path');const root=__dirname;const read=p=>fs.readFileSync(path.join(root,p),'utf8');let html=read('src/shell.html');for(const [token,file] of [['/* STYLE */','src/style.css'],['/* ENGINE */','src/engine.js'],['/* UI */','src/ui.js']])html=html.replace(token,()=>read(file));fs.writeFileSync(path.join(root,'index.html'),html);console.log('Built standalone index.html');
